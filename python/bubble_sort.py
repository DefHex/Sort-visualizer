def bubble_sort(arr):
    # length of array
    n = len(arr)
    # the scope of
    for i in range(n - 1):
        for j in range(n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
    return arr
